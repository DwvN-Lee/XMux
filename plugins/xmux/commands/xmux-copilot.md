---
description: Add or operate a Copilot teammate in the active XMux team
argument-hint: [teammate request]
allowed-tools: [Bash, Read]
---

# /xmux-copilot

Use the `xmux-copilot` skill to prepare or operate a Copilot teammate under Codex lead.

User request:

```text
$ARGUMENTS
```

Prefer the `xmux copilot` entrypoint for runtime orchestration. Ensure the Copilot HTTP MCP bridge is active before expecting mailbox responses.
