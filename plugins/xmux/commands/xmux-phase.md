# /xmux-phase

Use phased XMux workflow for staged design, implementation, and verification.

Prefer the `xmux-phase` skill when available. Keep Codex as the lead and use
XMux teammates only through the mailbox and wrapper commands so request ids,
responses, and runtime state remain traceable.
