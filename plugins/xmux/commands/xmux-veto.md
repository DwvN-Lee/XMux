# /xmux-veto

Use XMux VETO workflow for independent review, consensus checks, and
generator-validator separation.

Prefer the `xmux-veto` skill when available. Keep decisions grounded in XMux
mailbox responses, preserve request ids, and do not bypass the wrapper
lifecycle commands for teammate state.
