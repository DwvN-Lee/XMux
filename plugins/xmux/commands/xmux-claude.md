---
description: Add or operate a Claude teammate in the active XMux team
argument-hint: [teammate request]
allowed-tools: [Bash, Read]
---

# /xmux-claude

Use the `xmux-claude` skill to prepare or operate a Claude teammate under Codex lead.

User request:

```text
$ARGUMENTS
```

Prefer the `xmux claude` entrypoint for runtime orchestration, then use XMux MCP/mailbox APIs for task delivery and response collection.
