---
description: Add or operate a Gemini teammate in the active XMux team
argument-hint: [teammate request]
allowed-tools: [Bash, Read]
---

# /xmux-gemini

Use the `xmux-gemini` skill to prepare or operate a Gemini teammate under Codex lead.

User request:

```text
$ARGUMENTS
```

Prefer the `xmux gemini` entrypoint for runtime orchestration, then use XMux MCP/mailbox APIs for task delivery and response collection.
