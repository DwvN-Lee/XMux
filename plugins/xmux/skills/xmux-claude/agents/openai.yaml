interface:
  display_name: "xmux-claude"
  short_description: "Use Claude as an XMux teammate."
  default_prompt: "Use $xmux-claude to configure a single Claude teammate and route critique, requirements review, and edge-case exploration through MCP."
